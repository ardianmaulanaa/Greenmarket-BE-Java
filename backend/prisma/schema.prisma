generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        Int      @id @default(autoincrement())
  username  String
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  role      Role     @default(BUYER)
  products  Produk[]
  wishlists Wishlist[]
}

model Kategori_Produk {
  id_kategori   String   @id @default(uuid())
  nama_kategori String
  products      Produk[]
}

model Produk {
  id_produk      String          @id @default(uuid())
  id_user_seller Int
  id_kategori    String
  nama_produk    String
  deskripsi      String
  harga          Int
  detail         Produk_Detail?
  stok           Int
  status_produk  String
  created_at     DateTime        @default(now())
  kategori       Kategori_Produk @relation(fields: [id_kategori], references: [id_kategori])
  seller         User            @relation(fields: [id_user_seller], references: [id])
  fotos          Produk_Foto[]
  wishlists      Wishlist[]
}

model Produk_Detail {
  id_detail        String   @id @default(uuid())
  id_produk        String   @unique
  konten_deskripsi String   @db.Text // Tempat teks ketikan manual Anda
  catatan_penjual  String?
  produk           Produk   @relation(fields: [id_produk], references: [id_produk])
}

model Produk_Foto {
  id_foto   String @id @default(uuid())
  id_produk String
  url_foto  String
  produk    Produk @relation(fields: [id_produk], references: [id_produk], onDelete: Cascade)
}

model Wishlist {
  id_wishlist String   @id @default(uuid())
  id_user     Int
  id_produk   String
  created_at  DateTime @default(now())
  user        User     @relation(fields: [id_user], references: [id], onDelete: Cascade)
  produk      Produk   @relation(fields: [id_produk], references: [id_produk], onDelete: Cascade)
  @@unique([id_user, id_produk])
}

model Alamat {
  id_alamat      String @id @default(uuid())
  id_user        Int
  nama_penerima  String
  nomor_hp       String
  alamat_lengkap String
}

enum Role {
  BUYER
  SELLER
  ADMIN
}
